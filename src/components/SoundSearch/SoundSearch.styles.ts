import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';

export const StyledHeaderContainer = styled(Box)(() => ({
  textAlign: 'center',
}));

export const StyledFavoritesButtonContainer = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'center',
  marginBottom: 16,
}));
